<div>
  <p><strong>messages:</strong></p>

  <div id="schedules">
    <%= f.fields_for :messages do |message| %>
      <%= render 'message_fields', f: message %>
    <% end %>

    <div class="links">
      <%= link_to_add_association 'add messages', f, :messages %>
    </div>
  </div>
</div>
