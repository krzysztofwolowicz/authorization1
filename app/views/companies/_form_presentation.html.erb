<div>
  <p><strong>presentations:</strong></p>

  <div id="schedules">
    <%= f.fields_for :presentations do |presentation| %>
      <%= render 'presentation_fields', f: presentation %>
    <% end %>

    <div class="links">
      <%= link_to_add_association 'add presentations', f, :presentations %>
    </div>
  </div>
</div>
